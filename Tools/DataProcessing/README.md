# DataProcessing

Basic visualization of exported BioMetrix session data.

## Setup

```bash
python -m venv venv
```

```bash
.\venv\Scripts\activate
```

```bash
pip install -r requirements.txt

```
pip install pandas matplotlib

```bash
set JUPYTER_RUNTIME_DIR=%TEMP%\jupyter_runtime


## Run

```bash
jupyter notebook
```

Open `BasicVisualization.ipynb`, set `JSON_PATH` to your export file, then **Run All Cells**.
